<UserControl x:Class="Benchmarker.MVVM.View.Account.LoggedInView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Benchmarker.MVVM.View.Account"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Label Content="{Binding LoginMessage}" 
                   HorizontalAlignment="Center"
                   FontSize="14"/>
            <Button Content="Logout" Margin="10" Padding="15, 5" FontSize="14" Command="{Binding LogoutCommand}" Height="28"/>
        </StackPanel>
    </Grid>
</UserControl>
